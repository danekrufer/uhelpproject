# config/default.conf
server {
    listen 80;
    
    # Корень — сразу папка public
    root /usr/share/nginx/html/public;
    
    # Индексный файл — index.html внутри public
    index index.html;
    
    location / {
        try_files $uri $uri/ /index.html;
    }
}
